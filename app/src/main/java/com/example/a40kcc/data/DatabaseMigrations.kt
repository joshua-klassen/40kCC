package com.example.a40kcc.data

import androidx.room.migration.Migration

/**
 * Place to collect Room Migration objects. During development this array may be empty,
 * but keeping a central place simplifies adding migrations as the schema evolves.
 */
object DatabaseMigrations {
    // Fill this array with Migration objects, e.g. Migration(1,2) { /*...*/ }
    val MIGRATIONS: Array<Migration> = arrayOf()
}

